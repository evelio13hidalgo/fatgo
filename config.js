// Fatgo cloud config — Supabase project keys.
// Create a free project at https://supabase.com, then paste the values from
// Project Settings → API here. The anon key is safe to publish (it's public
// by design; row-level security protects the data).
// Leave both empty to run in device-only guest mode.
window.FATGO_CONFIG = {
  supabaseUrl: "",      // e.g. "https://abcdefgh.supabase.co"
  supabaseAnonKey: "",  // the long "anon / public" key
};
